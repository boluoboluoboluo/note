### 基础

```go
package common

//开始
func start_ex(){
	//段注释
    // #查看版本

	// #查看已安装的模块 (列表，路径)

	// #设置模块安装路径

	// #安装模块

	// #卸载模块

	// #引入模块
	//方式一：import xxx
	//方式二：import (
	// "xx/xx"
	// "yy/yy"
	//)

    // #调试模式，运行模式

    // #main方法
    //需文件顶部为package main
    //文件里main方法：func main(){}

    // #终端编译运行命令
    //编译命令：go build filename	//会编译生成exe文件
    //运行命令：go run xxx.go		//调试用此命令即可

    //关于引号
    //双引号""代表字符串string，其实质是一个byte类型的数组
    //单引号''表示rune类型,(不是字符串)
    //反引号``，用来创建原生的字符串字面量，它可以由多行组成，但不支持任何转义序列。
    //双引号和反引号之间才可以拼接

    //#全局变量首字母必须大写，小写为私有变量
    //#方法左大括号不能单独起一行
    //结构体首字母大写才能被外部调用 
    //type T struct{
    //	a int
    //	b float
    //}	
}

func common_ex(){
	// #变量定义 常见类型变量，局部变量，私有变量，常量
	var i = 3
	j := 4		//此方式只用于函数体
	var f = 3.0
	var s = "abc"
	var arr = [1,2,3]
    // #打印(整型，浮点，字符串，json，数组，集合等)
	// fmt.Println("hello world")

    // #变量类型

    // #if语句

    // #switch语句

    // #while语句

    // #循环 start
    // #循环 end

    // #随机数

    // #暂停3秒

    // #输入函数，回车结束

    // #退出，默认0正常

    // #调用全局表量

    // #调用其他方法

    // #new对象

    // #调用对象方法，属性
}
```

