### 文件方法

```go
package file_about


// #文件方法
func file_ex(){
	sfile := "s.txt"		//源文件
	dfile := "d.txt"		//目的文件

	// #判断目录是否存在，获取目录路径
	
	// #目录遍历
	
	// #目录创建，复制，移动，删除
	
	// #判断文件是否存在，获取文件路径，文件大小
	
	// #文件创建，复制，移动，删除
	
	// #打开文件 --只读，写入，追加，读写。编码方式
	
	// #读取文件 --字节，字符流，buf，读取，逐行读，全部读
	
	// #写入文件 --字节，字符流，buf，写入，写入行，写入全部
	
	// #换行符判断，文件末尾判断
	
	// #关闭文件，异常捕获，释放连接
	
	// #文件指针
	
	// #文件权限，设置文件权限

}
```

